{
  "_doc": "Revert-verify mutants for issue #4350 — QueryPrefixInjector. Two regexes: the used-prefix scan (which declared the tail `macbook:` of `device-work-macbook:`) and the declared-prefix scan (which, on `\\\\w+`, did not see a hand-declared hyphenated PREFIX and declared it a second time). `red_re` anchors on the `● <suite> › [Xn]` failure header, not on a bare key: jest echoes neighbouring `it(...)` source lines in its diff context (integration-test-revert-verify §A44/§A104).",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules",
    "packages/test-utils/node_modules",
    "packages/obsidian-plugin/node_modules",
    "packages/services/node_modules"
  ],
  "red_re": "● .*› \\[([A-Z]\\d+)\\]",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/utils/QueryPrefixInjector.ts",
  "harness": "cd \"$(dirname {subject})/../../../../packages/cli\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/unit/utils/namespace-prefix-parity-4350.test.ts tests/integration/hyphen-namespace-4350.integration.test.ts --verbose 2>&1",
  "mutants": {
    "M7_used_scan_keeps_old_shape": {
      "from": "    `(?<![:\\\\w])(${PREFIX_PATTERN_SOURCE}):[a-zA-Z_]`,",
      "to": "    `(?<![:\\\\w])([a-z][a-zA-Z0-9]*):[a-zA-Z_]`,",
      "expect": ["Q1"]
    },
    "M8_declared_scan_keeps_word_only": {
      "from": "  const prefixPattern = /PREFIX\\s+([\\w-]+)\\s*:/gi;",
      "to": "  const prefixPattern = /PREFIX\\s+(\\w+)\\s*:/gi;",
      "expect": ["Q2"]
    }
  }
}
