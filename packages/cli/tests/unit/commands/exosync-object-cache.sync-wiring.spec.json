{
  "_doc": "Revert-verify for req 086df113 at the SECOND wiring site: `exosync sync`. The parity spec pins only its own call site, so deleting the wiring from the MAIN command would red nothing without this file (review finding on WIP 1281b62b). Axes B9/B10 in exosync-object-cache.test.ts. ⛔ Якоря починены 2026-09-26: их сломал НЕ дрейф времени, а СОСЕДНЯЯ ветка (req af002ec4, conditional requests) — она сменила первый аргумент wireObjectCache с rawTransport на conditionalTransport. spec-anchor-sweep.py показывал count 0, jest-оси при этом были зелены: отказал слой ДОКАЗАТЕЛЬСТВА, не защита.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/commands/exosync-sync.ts",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "harness": "cd \"$(dirname {subject})/../..\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/unit/commands/exosync-object-cache.test.ts 2>&1",
  "red_re": "› (B\\d+\\w*) ",
  "mutants": {
    "M1_sync_wiring_removed": {
      "_note": "`exosync sync` сохраняет корректный, но НЕкэширующий транспорт. ⛤ Якорь — ТОЛЬКО строка деструктуризации, БЕЗ аргумента. Прежняя редакция захватывала вызов целиком вместе с первым аргументом, и соседняя фича (req af002ec4) сменила именно аргумент (rawTransport → conditionalTransport) плюс reflow prettier ⇒ count стал 0 МОЛЧА, при исправных осях. Аргумент — то, что меняет следующий декоратор; строка деструктуризации — нет.",
      "from": "  const { transport: readTransport, cache: objectCache } = wireObjectCache(\n",
      "to": "  const readTransport = conditionalTransport;\n  const objectCache = null;\n  void wireObjectCache(\n",
      "expect": ["B9"]
    },
    "M2_sync_flag_ignored": {
      "_note": "`--no-object-cache` stops reaching the wiring on the sync path.",
      "from": "    ...(opts.objectCache !== undefined ? { enabled: opts.objectCache } : {}),\n",
      "to": "",
      "expect": ["B10"]
    }
  }
}
