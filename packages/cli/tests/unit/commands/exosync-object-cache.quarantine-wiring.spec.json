{
  "_doc": "Revert-verify for req 086df113 at the THIRD wiring site: `exosync quarantine`. Axis B11 in exosync-object-cache.test.ts.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/commands/exosync-quarantine.ts",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "harness": "cd \"$(dirname {subject})/../..\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/unit/commands/exosync-object-cache.test.ts 2>&1",
  "red_re": "› (B\\d+\\w*) ",
  "mutants": {
    "M1_quarantine_wiring_removed": {
      "_note": "The quarantine resolver re-reads blobs the sync that created the conflict already fetched — without the wiring it pays for them twice.",
      "from": "  const { transport } = wireObjectCache(rawTransport, {\n    ...(opts.objectCache !== undefined ? { enabled: opts.objectCache } : {}),\n    sha1: nodeSha1,\n  });\n",
      "to": "  const transport = rawTransport;\n",
      "expect": ["B11"]
    }
  }
}
