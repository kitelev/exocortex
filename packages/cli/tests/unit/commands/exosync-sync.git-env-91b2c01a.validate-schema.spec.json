{
  "_doc": "Revert-verify for req 91b2c01a, subject = validate-schema.ts, the one allow-listed inheritor. The allow-list names the CALL (`git diff --cached`), not the file, so a second inheriting git call in the same file must fail H2. Run from the repo root: python3 -I ~/.claude/bin/mutant-driver.py <this spec> after setting `tree` and `subject` to YOUR checkout.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "harness": "cd \"$(dirname {subject})/../..\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/unit/commands/exosync-sync.test.ts -t 'H[0-9] ' 2>&1",
  "red_re": "› (H\\d+\\w*) ",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/commands/validate-schema.ts",
  "mutants": {
    "M10_second_git_call_inherits": {
      "from": "    const stdout = execSync(\"git diff --cached --name-only\", {\n",
      "to": "    execSync(\"git rev-parse HEAD\", { cwd: vaultPath });\n    const stdout = execSync(\"git diff --cached --name-only\", {\n",
      "expect": ["H2"]
    }
  }
}
