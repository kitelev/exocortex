{
  "_doc": "Revert-verify mutants for ticket 7a84b9f0 (req 81cd5d1f-6466-47c9-99cd-8fe9f9b500a3), subject packages/cli/src/commands/sparql-query.ts. Sibling of query-skipped-files-7a84b9f0.loader.spec.json (the driver mutates one file per spec). These two mutants separate the command's TWO surfaces: the printed stderr block and the machine-readable meta. Each must be reachable on its own — a suite in which one mutant reddens both surfaces would not prove they are independently wired.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/commands/sparql-query.ts",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "harness": "cd \"$(dirname {subject})/../../../../packages/cli\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/integration/query-skipped-files-7a84b9f0.integration.test.ts 2>&1",
  "red_re": "› ([A-Z]\\d+\\w*) ",
  "mutants": {
    "M2_notice_computed_but_never_printed": {
      "_note": "The wiring axis (feature-sdd Step 4 §a/§b): skippedFilesNotice can be perfect and still reach nobody. Type-preserving — the condition keeps its shape, only its truth changes, so no narrowing is lost.",
      "from": "        if (skippedNotice !== null) {\n          console.error(skippedNotice);\n        }",
      "to": "        if (skippedNotice !== null && skippedNotice === undefined) {\n          console.error(skippedNotice);\n        }",
      "expect": ["S2", "S4", "S5", "S6", "S7", "S8"]
    },
    "M3_select_meta_drops_the_loader_fields": {
      "_note": "The JSON surface alone. Anchored on `itemCount: results.length` because the construct branch is otherwise byte-identical; the SELECT query these axes run takes this branch.",
      "from": "              itemCount: results.length,\n              loadDurationMs: loadDuration,\n              execDurationMs: execDuration,\n              triplesScanned: triples.length,\n              cacheHit,\n              queryResultCacheHit: false,\n              ...loaderMeta,",
      "to": "              itemCount: results.length,\n              loadDurationMs: loadDuration,\n              execDurationMs: execDuration,\n              triplesScanned: triples.length,\n              cacheHit,\n              queryResultCacheHit: false,",
      "expect": ["S1", "S7", "S8"]
    }
  }
}
