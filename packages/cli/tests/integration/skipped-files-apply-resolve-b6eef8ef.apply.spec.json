{
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "red_re": "› (V\\d+\\w*) ",
  "_doc": "Revert-verify for req b6eef8ef (#4274), subject packages/cli/src/commands/apply.ts. Axes V1-V5 in packages/cli/tests/integration/skipped-files-apply-resolve-b6eef8ef.integration.test.ts. Run from the repo root: python3 -I ~/.claude/bin/mutant-driver.py <this spec> after setting `tree` and `subject` to YOUR checkout (with submodules initialised).",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/cli/src/commands/apply.ts",
  "harness": "cd \"$(dirname {subject})/../..\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/integration/skipped-files-apply-resolve-b6eef8ef.integration.test.ts -t 'V[0-9] ' 2>&1",
  "mutants": {
    "M1_apply_skipped_list_never_printed": {
      "from": "          if (skippedNotice !== null) {\n            process.stderr.write(`${skippedNotice}\\n`);\n",
      "to": "          if (skippedNotice !== null && skippedNotice === undefined) {\n            process.stderr.write(`${skippedNotice}\\n`);\n",
      "expect": ["V2"]
    },
    "M2_apply_target_line_never_printed": {
      "from": "            if (targetNotice !== null) {\n              process.stderr.write(`${targetNotice}\\n`);\n",
      "to": "            if (targetNotice !== null && targetNotice === undefined) {\n              process.stderr.write(`${targetNotice}\\n`);\n",
      "expect": ["V1", "V4"],
      "_note": "V4 is red too, legitimately: the target line is ONE code path for both load modes; on a cache hit it prints the cause-less variant, which V4 asserts. My first expectation listed only V1."
    }
  }
}
