{
  "_doc": "Revert-verify mutants for issue #4403 — normalizeIRI converts TERM IRIs only. Type-preserving mutants (§A36); red_re anchors on the `● <suite> › [Xn]` header.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules",
    "packages/test-utils/node_modules",
    "packages/obsidian-plugin/node_modules",
    "packages/services/node_modules"
  ],
  "red_re": "● .*› \\[([A-Z]\\d+)\\]",
  "harness": "cd \"$(dirname {subject})/../../../../packages/core\" && node ../../node_modules/jest/bin/jest.js tests/unit/utilities/FrontmatterService.normalize-iri-4403.test.ts --verbose 2>&1",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/core/src/utilities/FrontmatterService.ts",
  "mutants": {
    "M1_vault_url_shape_restored": {
      "_note": "normalizeIRI applies the unanchored vault-URL shape again (the pre-#4403 `iriToObsidianName` fallback): a value with `#` and a `/<name>.md` tail becomes `[[<name>]]`.",
      "from": "    return term ? `${term.namespace.prefix}__${term.localName}` : property;",
      "to": "    return term ? `${term.namespace.prefix}__${term.localName}` : (/\\/([^/]+)\\.md$/.exec(property)?.[1] ?? property);",
      "expect": ["N1", "N2", "N4"]
    },
    "M2_term_branch_dropped": {
      "_note": "The term-IRI branch never fires: term IRIs stop being canonicalised as keys and wikilinked as values.",
      "from": "    return term ? `${term.namespace.prefix}__${term.localName}` : property;",
      "to": "    return term && property.length < 0 ? `${term.namespace.prefix}__${term.localName}` : property;",
      "expect": ["N3", "N4"]
    }
  }
}
