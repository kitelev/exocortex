{
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules"
  ],
  "harness": "ROOT=\"${SUT%%/packages/core/*}\"; cd \"$ROOT\" && npx jest --config packages/core/jest.config.js tests/domain/display-name/nestedDisplayName.test.ts 2>&1; cd \"$ROOT/packages/cli\" && node --experimental-vm-modules ../../node_modules/jest/bin/jest.js tests/integration/resolve-display-name.nestedDisplayName.integration.test.ts 2>&1",
  "red_re": "› @req:\\S+ ([VW]\\d+) ",
  "_doc": "req 0f992e88 (#4303) — the engine half: the new value source and its POSITION among alias / label / bare linkpath.",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/core/src/domain/display-name/DisplayNameTemplateEngine.ts",
  "mutants": {
    "M1_engine_hop_removed": {
      "from": "    if (preferComposed) {\n      const preferred = composed();\n      if (preferred !== null) return preferred;\n    }\n\n    if (label !== null) return label;\n\n    if (!preferComposed) {\n      const fallback = composed();\n      if (fallback !== null) return fallback;\n    }\n\n",
      "to": "    void composed;\n    if (label !== null) return label;\n\n",
      "expect": ["V1", "W1"],
      "_note": "TYPE-PRESERVING: `void composed;` keeps the closure referenced (noUnusedLocals) while no call site remains, so the suite still compiles and the failure read is about the AXES, not about the mutant (integration-test-revert-verify §A36)."
    },
    "M2_engine_hop_before_label": {
      "from": "    if (label !== null) return label;",
      "to": "    {\n      const first = composed();\n      if (first !== null) return first;\n    }\n    if (label !== null) return label;",
      "expect": ["V2", "W2"]
    },
    "M3_engine_hop_before_alias": {
      "from": "    if (alias) {\n      return alias;\n    }",
      "to": "    if (alias) {\n      const early = this.options.nestedDisplayName?.(value, undefined);\n      if (early !== null && early !== undefined && early.trim() !== \"\") return early.trim();\n      return alias;\n    }",
      "expect": ["V4"]
    },
    "M4_engine_empty_composed_accepted": {
      "from": "      return rendered !== null && rendered !== undefined && rendered.trim() !== \"\"\n        ? rendered.trim()\n        : null;",
      "to": "      return rendered !== null && rendered !== undefined ? rendered.trim() : null;",
      "expect": ["V9"]
    }
  }
}
