{
  "_doc": "Revert-verify mutants for ticket 6572f3f3, req 38e3f174-4a05-4743-a2f4-c7ec2c711202 — SUBJECT: packages/core/src/utilities/FrontmatterService.ts. Two guards in this file's diff (§A8): the hash early-return that survives the removal, and the ABSENCE of the nine-entry table. ⛤ MF2 is the honest mutant for a STRUCTURAL axis: removing the table is behaviourally neutral BY CONSTRUCTION (the guard had already made both branches agree on all 14 measured input forms), so no behavioural mutant can distinguish it — re-adding the table is what IV11 must catch, and saying that plainly beats dressing the axis up as behavioural (§A35). All TYPE-PRESERVING. `expect` filled ONLY from a completed run.",
  "tree": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>",
  "subject": "<ABSOLUTE PATH TO YOUR exocortex CHECKOUT>/packages/core/src/utilities/FrontmatterService.ts",
  "symlink_node_modules": [
    "node_modules",
    "packages/cli/node_modules",
    "packages/core/node_modules",
    "packages/obsidian-plugin/node_modules",
    "packages/req-audit/node_modules",
    "packages/test-utils/node_modules"
  ],
  "red_re": "› ([A-Z]{1,3}\\d+[a-z]?) ",
  "harness": "cd \"$(dirname {subject})/../../../../packages/core\" && node ../../node_modules/jest/bin/jest.js tests/utilities/FrontmatterService.normalizeIRI.test.ts tests/utilities/FrontmatterService.test.ts tests/utilities/FrontmatterService.applyPatch.test.ts 2>&1",
  "mutants": {
    "MF1_hash_early_return_removed": {
      "_note": "The early return is the only thing keeping iriToObsidianName's SECOND shape (vault URL → basename) out of the physical-write-key path. Measured on origin/main 0857307b BEFORE this ticket: removing it reddened NOTHING across 132 tests in 4 suites with a green control — the property was true but UNLOCKED, and IV12 is its first specification.",
      "from": "    if (property.lastIndexOf(\"#\") < 0) return property;",
      "to": "    if (property.lastIndexOf(\"#\") < -1) return property;",
      "expect": [
        "IV12"
      ]
    },
    "MF2_static_prefix_table_reintroduced": {
      "_note": "Re-add a second literal list of bases as a hot path — exactly what the ticket removed and what Namespace.fromTermIRI's docstring warns against. Behaviour stays identical (the table is guarded by the same rule), so ONLY the structural axis IV11 may redden: that is the point, and it is why IV11 is called structural.",
      "from": "  static normalizeIRI(property: string): string {",
      "to": "  private static readonly IRI_PREFIX_MAP: Record<string, string> = {\n    \"https://exocortex.my/ontology/ems#\": \"ems__\",\n  };\n\n  static normalizeIRI(property: string): string {",
      "expect": [
        "IV11"
      ]
    }
  }
}
