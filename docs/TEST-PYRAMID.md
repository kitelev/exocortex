# Test Pyramid Policy

> **Moved.** This content was consolidated into the canonical testing guide.
> See **[../TESTING.md](../TESTING.md)** → section **[Test Architecture → Test Pyramid Policy](../TESTING.md#test-architecture)**
> for the pyramid structure, layer ratios, enforcement status, "when to add each
> test type", and the coverage-regression checklist.

This stub is intentionally kept in place because code comments reference it by
path (`packages/obsidian-plugin/jest.config.js`, `packages/cli/jest.config.js`).
For the source of truth on thresholds and counts, see
[../TESTING.md → Coverage Gates](../TESTING.md#coverage-gates).
